const Container = (theme) => ({
    MuiContainer: {
        styleOverrides: {
            root: {
                display: 'flex',
                padding: 0
            }
        }
    },
})

export default Container