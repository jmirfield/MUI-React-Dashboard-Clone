const IconButton = (theme) => ({
    MuiIconButton: {
        styleOverrides: {
            root: {
                width: '44px',
                height: '44px'
            }
        }
    },
})

export default IconButton